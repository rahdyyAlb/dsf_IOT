{% extends 'base.html.twig' %}

{% block title %}Edition Transactions{% endblock %}

{% block body %}
<div class="text-center d-flex align-items-center justify-content-center align-middle centered">

    <div class="text-center align-items-center justify-content-center">

        <div class ="card card-body text-center align-items-center justify-content-center width-80">
            <h1 class="m-2 pb-3 text-center">Edition de la transaction</h1>

            {{ include('transactions/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

            {{ include('transactions/_delete_form.html.twig') }}

            <a href="{{ path('app_transactions_index') }}">Retour</a>
        </div>
    </div>
</div>
{% endblock %}
