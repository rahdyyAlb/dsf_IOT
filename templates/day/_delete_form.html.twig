<form method="post" action="{{ path('app_day_delete', {'id': day.id}) }}" onsubmit="return confirm('Voulez vous vraiment supprimer ce jour?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ day.id) }}">
    <button class="btn">Supprimer</button>
</form>
