<form method="post" action="{{ path('app_customers_delete', {'id': customer.id}) }}"
      onsubmit="return confirm('Voulez vous supprimer ce client?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ customer.id) }}">
    <button class="btn btn-danger mt-2 m-2">Supprimer</button>
</form>
