{% extends 'base.html.twig' %}

{% block title %}TransactionIteme{% endblock %}

{% block body %}
    <div class="text-center d-flex align-items-center justify-content-center align-middle centered">

        <div class="text-center align-items-center justify-content-center">

            <div class="card card-body text-center align-items-center justify-content-center width-80">
                <h1 class="m-2 pb-3 text-center">Obj transactions</h1>

                <table class="table">
                    <tbody>
                    <tr>
                        <th>ID</th>
                        <td>{{ transaction_iteme.id }}</td>
                    </tr>
                    <tr>
                        <th>Quantité</th>
                        <td>{{ transaction_iteme.quantity }}</td>
                    </tr>
                    <tr>
                        <th>Total</th>
                        <td>{{ transaction_iteme.total }}</td>
                    </tr>
                    </tbody>
                </table>

                <div class="align-items-center justify-content-between align-self-center">
                    <a href="{{ path('app_transaction_iteme_edit', {'id': transaction_iteme.id}) }}">Editer</a>
                    {{ include('transaction_iteme/_delete_form.html.twig') }}
                    <a href="{{ path('app_transaction_iteme_index') }}">Retour</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
